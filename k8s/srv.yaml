apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-srv
  namespace: 4sh-formation-@ME
  labels:
    app: app-srv
    kind: app
spec:
  selector:
    matchLabels:
      app: app-srv
  replicas: 1
  template:
    metadata:
      labels:
        app: app-srv
    spec:
      containers:
        - name: app-srv
          image: europe-docker.pkg.dev/quatreapp/k8straining/myapp-srv-@ME
          resources:
            requests:
              cpu: "100m"
              memory: "500Mi"
            limits:
              cpu: "500m"
              memory: "1000Mi"
          livenessProbe:
            httpGet:
              path: /healthcheck
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 10
          command: ["/usr/local/conduit/bin/conduit","--env", "@ME"]
          ports:
            - containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: app-srv
  namespace: 4sh-formation-@ME
spec:
  selector:
    app: app-srv
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000
  type: ClusterIP
---
